{{#each this}}
  <div class="comment-container">
    <div class="comment">
      <p class="comment-body">
        {{! why is it not rendering }}
        {{user.username}}
        {{comment}}
        on
        {{formatDate created_at}}</p>
    </div>
  </div>

{{/each}}

